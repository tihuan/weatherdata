$("body").removeClass();
$("body").addClass("<%= @city.name.downcase.tr_s(' ', '-') if @city %>");
$("#show-page").html("<%= escape_javascript(render partial: 'show', locals: { cities: @cities, city: @city } ) %>");
